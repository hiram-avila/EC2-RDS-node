name: Ejecutar pruebas

on: 
  push:
    branches:
      - main # Reemplaza 'main' con la rama principal de tu repositorio

jobs:
  Ejecutar_tests:
    runs-on: ubuntu-latest 
    steps:
    - name: Checkout del codigo 
      run: |
        echo "hola hiram"